# DestiFramework
A collection of functions to perform mathematical operations often needed in a destillery


# Installing
## Getting Started Windows:
1. Download the latest release from dist/ folder (Windwos releases end with "w")
2. To run the Programm, simply execute the main.exe file in the downloaded Folder

## Getting Started Linux:
1. Same as on Windows, but the file ends with "l"
2. Same as on Windows, but its a Linux executable.

# How to use:
When running the Program, a Shell Window will open.
It will prompt you to enter specified characters to select the modes.
For example:
When you need to do alcohol calculations: enter an "a" and press enter, the programm will enter the alcohol menu.
